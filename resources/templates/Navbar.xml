<?xml version="1.0" encoding="utf-8"?>
<messages>

	<message id="navbar">
		<div class="relative flex h-12 items-center border border-gray-500 bg-gray-800 px-2 dark:border-gray-100">
        ${hamburger-button class="sm:hidden bg-gray-800 text-white border-0"}
        ${logo class="mx-auto h-8 w-auto sm:mx-0"} ${menu class="w-full absolute
        right-0 top-12 flex flex-col ps-0 !m-0 items-center bg-gray-800
        sm:static sm:flex sm:ml-3 sm:w-auto sm:flex-row sm:space-x-4 list-none
        text-gray-50"}
			<div class="sm:ms-auto">
				<button class="flex rounded-full border-0 bg-gray-800 p-0 text-sm focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-800">
            ${user-image class="h-8 w-8 rounded-full"}
				</button>
          ${user-menu-wrapper class="hidden absolute right-0 z-10 mt-2 w-32
          origin-top-right rounded-md bg-basic py-1 shadow-lg ring-1 ring-black
          ring-opacity-5 focus:outline-none"}
			</div>
		</div>
	</message>

	<message id="user-menu-wrapper">
      ${user-menu} ${theme-switcher class="relative block w-10 h-5 ms-auto
      bg-white rounded-full shadow-lg border-0"}
	</message>

</messages>